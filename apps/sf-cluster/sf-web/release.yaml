apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: angi-helloworld
  namespace: angi
spec:
  interval: 1m
  chart:
    spec:
      chart: angi-helloworld
      sourceRef:
        kind: GitRepository
        name: angi-helloworld
  values:
    image:
      tag: 0.0.2